import React from 'react'
//Appears when page is loading, 
//put it in the folder/page where we want to show it. 
//Also same with the error
const Loading = () => {
  return (
    <div>Loading...</div>
  )
}

export default Loading